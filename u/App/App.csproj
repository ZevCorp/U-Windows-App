<Project Sdk="Microsoft.NET.Sdk">
  <!-- Ü DESDE CERO: la burbuja que flota, la voz, y nada más (spec 052). -->
  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net8.0-windows10.0.19041.0</TargetFramework>
    <UseWPF>true</UseWPF>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <AssemblyName>U-nuevo</AssemblyName>
    <RootNamespace>U.Nuevo</RootNamespace>
    <ApplicationManifest>app.manifest</ApplicationManifest>
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="..\Nucleo\Nucleo.csproj" />
    <!-- La misma versión que usa main: probada en esta máquina con el micrófono y el altavoz de verdad. -->
    <PackageReference Include="NAudio" Version="2.2.1" />
  </ItemGroup>
</Project>
