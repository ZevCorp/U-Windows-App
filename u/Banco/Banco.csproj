<Project Sdk="Microsoft.NET.Sdk">
  <!-- EL BANCO DEL CICLO (spec 052, nivel 4): el ciclo nuevo sobre el escritorio real, con Jev de verdad y
       el ratón de verdad, fase por fase. Las mismas tareas con las que se midió main, para comparar. -->
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0-windows10.0.19041.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <AssemblyName>banco-u</AssemblyName>
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="..\Nucleo\Nucleo.csproj" />
  </ItemGroup>
</Project>
